import React from 'react'

const ShopListing = () => {
  return (
    <Container>
       {loading ? (
        <CircularProgress />
      ) : (
        <>
        <Filters>
          <Menu>
            {filter.map((filters) => (
              <FilterSection>
                 <Title>{filters.name}</Title>
                  {filters.value === "price" ? (
                    
      <Container/>
  )
}

export default ShopListing