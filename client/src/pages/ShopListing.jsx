import React from 'react'

const ShopListing = () => {
  return (
    <Container>
       {loading ? (
        <CircularProgress />
      ) : (
        <>
        <Filters>
          <Menu>
            {filter.map((filters) => (
              <FilterSection>
      
      <Container/>
  )
}

export default ShopListing